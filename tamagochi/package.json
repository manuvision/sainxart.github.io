{
  "name": "tamagochi-pocket-pet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "check": "node --check app.js && node --check device.js && node --check game.js && node --check lcd.js && node --check audio.js", "test": "node --test tests/*.test.js" }
}
